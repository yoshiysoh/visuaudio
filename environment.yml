name: visuaudio

dependencies:
  - defaults::python
  - defaults::line_profiler
  - defaults::memory_profiler
  - defaults::numba
  - defaults::numpy
  - defaults::pyaudio
  - defaults::pyopengl
  - defaults::pyqt
  - defaults::pyqtgraph
  - defaults::scipy
