name: visuaudio

dependencies:
  - defaults::python
  - defaults::colorcet
  - defaults::h5py
  - defaults::matplotlib
  - defaults::numba
  - defaults::numpy
  - defaults::pyopengl
  - defaults::pyqt
  - defaults::pyqtgraph
  - defaults::scipy
  - conda-forge::python-sounddevice
